## 2026-02-03 Task: Initialize learnings
- The file `rust/enhanced/login_cellphone.rs` implements NetEase Music API's cellphone login functionality.
- It uses `reqwest` for HTTP requests and `serde` for serialization/deserialization.
- It relies on `crate::enhanced::crypto::weapi` for data encryption.
- Structs follow the API's naming conventions (e.g., `ctcode`, `avatarImgIdStr`).
