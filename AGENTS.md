just focus on rust folder, don't modify other js or ts files

## Rust Module Conventions

Prohibit the creation of `mod.rs` files. Use the modern Rust module convention: use a file named `{directory_name}.rs` at the same level as the `{directory_name}/` directory instead of `{directory_name}/mod.rs`.
